@page "/"
@* @inject LockerSvc.LockerSvcClient LockerSvcClient
@rendermode @(new InteractiveServerRenderMode(false))

<PageTitle>Home</PageTitle>

@if (Lockers != null)
{
    @foreach(LockerMessage lockerMessage in Lockers)
    {
        <MudText Typo="Typo.h3" GutterBottom="true">@lockerMessage.Name</MudText>
    }
}

<MudButton>Ajouter</MudButton> *@